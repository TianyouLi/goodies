<test>
    <preconditions>
        <table_exists>hits</table_exists>
    </preconditions>

    <query><![CDATA[SELECT RegionID, COUNT(DISTINCT UserID) AS u FROM hits GROUP BY RegionID ORDER BY u DESC LIMIT 10;]]></query>
</test>
